spring:
  application:
    name: user
  datasource:
    url: jdbc:postgresql://localhost:5435/ms-user-ms
    username: postgres-user
    password: postgres-password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    addresses: amqps://jaragua.lmq.cloudamqp.com
    username: vtmsukyt
    password: 4ea7-luy1YJG2c67rJaDtk0L26fDqiRb
    virtual-host: vtmsukyt

    flyway:
      enabled: true
      locations: classpath:db/migration
      baseline-on-migrate: true

server:
  port: 8081